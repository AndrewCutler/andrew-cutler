import { Flex } from '@chakra-ui/react';
import React, { ReactElement } from 'react';

const Contact = (): ReactElement => {
  return <Flex paddingBottom={5}>(518) 275 9608</Flex>;
};

export default Contact;
