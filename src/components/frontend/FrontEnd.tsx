import { Box } from '@chakra-ui/react';
import { ReactElement } from 'react';
import StackSection from '../stack-section/StackSection';

const FrontEnd = (): ReactElement => <StackSection title='Front-end' />;

export default FrontEnd;
